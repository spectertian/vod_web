<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN"
        "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">
<html xmlns="http://www.w3.org/1999/xhtml">
<head>
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8">
    <title>MP4电影-系统提示</title>
</head>
<body data-new-gr-c-s-check-loaded="14.1043.0" data-gr-ext-installed="">
<table border="0" align="center" cellpadding="5" cellspacing="1"
       style="color:#333333;margin-top:100px;background:#70afd3">
    <tbody>
    <tr>
        <th style="color:#FFFFFF">操作成功！</th>
    </tr>
    <tr>
        <td style="background:#FFF; padding:20px 10px; font-size:12px; line-height:25px"><font
                    style="color:#FF0000;font-size:14px">留言成功，我们会尽快审核您的留言！</font><br>系统将在
            <span id="countDownSec" style="color:blue;font-weight:bold">3</span>&nbsp;秒后自动跳转，如果不想等待请<a
                    href="{{ path("message") }}" style="color:#069;">点击这里</a></td>
    </tr>
    </tbody>
</table>
<script language="javascript" type="text/javascript">
    var countDown = function (timer, eleId, interType) {
        document.getElementById(eleId).innerHTML = timer;
        var interval = interType == 's' ? 1000 : (interType == 'm' ? 1000 * 60 : 1000 * 60 * 60);
        window.setInterval(function () {
            timer--;
            if (timer > 0) {
                document.getElementById(eleId).innerHTML = timer;
            }
            if (timer == 0) {
                window.location.href="{{ path("message") }}";
            }
        }, interval);
    };
    countDown(3, 'countDownSec', 's');
</script>
</body>
<grammarly-desktop-integration data-grammarly-shadow-root="true"></grammarly-desktop-integration>
</html>